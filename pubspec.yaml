name: dynamic_popup
description: A flexible and customizable dynamic popup system for Flutter with markdown support and interactive components.
version: 1.0.5
homepage: https://github.com/asterd/dynamic_popup
repository: https://github.com/asterd/dynamic_popup
issue_tracker: https://github.com/asterd/dynamic_popup/issues

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_html: ^3.0.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
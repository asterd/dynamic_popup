name: dynamic_popup
description: A flexible and customizable dynamic popup system for Flutter with markdown support and interactive components.
version: 1.0.2
homepage: https://github.com/example/dynamic_popup

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_html: ^3.0.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: